version: 2.1
orbs:
  yarn-berry: checkmoney/yarn-berry@1.0.0
workflows:
  version: 2
  build:
    jobs:
      - yarn-berry/local:
          name: eslint
          command: 'lint:code'
          filters:
            branches:
              only: /.*/
            tags:
              only: /.*/
